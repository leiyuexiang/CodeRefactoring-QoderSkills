# P2 待执行报告：Service/DAO/Model 三层目录结构优化

## 基本信息

| 项目 | 内容 |
|------|------|
| 优先级 | P2（目录结构优化） |
| 影响模块 | `element-server-com`（主）、`element-server`（引用）、`grp-element-api`（跨模块引用） |
| 状态 | **待执行** — 需用户确认后逐层实施 |
| 生成日期 | 2026-03-17 |

---

## 概览

| 层级 | 当前问题 | 标准目标 | 涉及文件数 | import 影响 | 风险等级 |
|------|---------|---------|-----------|------------|---------|
| Service | 接口按业务分散，无 facade/ 统一目录 | `service/facade/` + `service/impl/` | ~116 | 289 处 / 156 文件 | 中 |
| DAO + Mapper | mapper/ 独立包，无 entity/ 目录 | `dao/mapper/` + `dao/entity/` | ~112 | 24 处 / 24 文件 | 高 |
| Model | 缺少 vo/、query/，根目录有未分类文件 | `model/dto/` + `model/vo/` + `model/query/` | ~10 | 379 处 / 253 文件 | 高（跨模块） |

---

## 一、Service 层 facade/impl 分离

### 1.1 当前结构问题

```
element-server-com/src/main/java/grp/pt/service/
├── IEleRelationService.java              ← 根目录接口（6个）
├── IElementValueSetService.java
├── IOrgFeignService.java
├── InformationCheckService.java
├── IProcessElementService.java
├── ValueControlManager.java
├── DBScripGenerateService.java
├── basedata/                             ← 按业务分组的接口（36个）
│   ├── IAgencyService.java
│   ├── IAnnualContrastService.java
│   ├── IApiElementService.java
│   ├── ... (共36个接口)
│   └── impl/                             ← 业务分组下的实现（30+个）
│       ├── AgencyServiceImpl.java
│       ├── AnnualContrastServiceImpl.java
│       └── ...
├── bookset/                              ← 账套分组（5个接口）
│   ├── IAccEleManagerService.java
│   ├── IAgencyEleManagerService.java
│   ├── IBookSetService.java
│   ├── IBookSetTypeService.java
│   ├── INoSessionBookSetService.java
│   └── impl/                             ← 账套实现（7个）
│       ├── AccEleManagerServiceImpl.java
│       └── ...
├── agencyManager/                        ← 单位管理分组
│   ├── IAgencyManagerService.java
│   ├── IAgencyManagerAffixService.java
│   ├── constant/                         ← 非标准子目录（常量、枚举等混入service包）
│   ├── enums/
│   ├── exception/
│   ├── feignclient/
│   ├── model/
│   ├── util/
│   └── impl/
├── impl/                                 ← 根级实现（11个）
│   ├── EleRelationServiceImpl.java
│   ├── ValueControlManagerImpl.java
│   ├── ProcessElementServiceImpl.java
│   ├── OrgFeignServiceImpl.java
│   ├── CommonBaseDataImportService.java
│   ├── DBScripGenerateServiceImp.java    ← 注意：Imp 而非 Impl
│   ├── ElementValueSetServiceImpl.java
│   ├── FinanceStandardInitUtil.java      ← 工具类放在service/impl下
│   ├── InformationCheckServiceImpl.java
│   ├── DatabaseSourceServiceEle.java
│   └── ...
├── executor/                             ← 执行器
│   └── ExecutorMonitorService.java
└── synchronization/                      ← 同步服务
    └── impl/
        └── SynDataServiceImpl.java
```

### 1.2 目标结构

```
service/
├── facade/                     # 所有 Service 接口统一归入
│   ├── IEleRelationService.java
│   ├── IElementValueSetService.java
│   ├── IOrgFeignService.java
│   ├── ... (原 service/ 根目录接口)
│   ├── IAgencyService.java
│   ├── IAnnualContrastService.java
│   ├── ... (原 service/basedata/ 接口)
│   ├── IAccEleManagerService.java
│   ├── ... (原 service/bookset/ 接口)
│   ├── IAgencyManagerService.java
│   └── ... (原 service/agencyManager/ 接口)
└── impl/                       # 所有 Service 实现统一归入
    ├── EleRelationServiceImpl.java
    ├── ValueControlManagerImpl.java
    ├── ... (原 service/impl/ 实现)
    ├── AgencyServiceImpl.java
    ├── AnnualContrastServiceImpl.java
    ├── ... (原 service/basedata/impl/ 实现)
    ├── AccEleManagerServiceImpl.java
    ├── ... (原 service/bookset/impl/ 实现)
    └── AgencyManagerServiceImpl.java
```

### 1.3 迁移清单

#### Service 接口迁移（→ service/facade/）

| 序号 | 类名 | 原包名 | 新包名 |
|------|------|--------|--------|
| 1 | IEleRelationService | grp.pt.service | grp.pt.service.facade |
| 2 | IElementValueSetService | grp.pt.service | grp.pt.service.facade |
| 3 | IOrgFeignService | grp.pt.service | grp.pt.service.facade |
| 4 | InformationCheckService | grp.pt.service | grp.pt.service.facade |
| 5 | IProcessElementService | grp.pt.service | grp.pt.service.facade |
| 6 | ValueControlManager | grp.pt.service | grp.pt.service.facade |
| 7 | DBScripGenerateService | grp.pt.service | grp.pt.service.facade |
| 8-43 | IAgencyService ~ IVirtualDataBaseService (36个) | grp.pt.service.basedata | grp.pt.service.facade |
| 44-48 | IAccEleManagerService ~ INoSessionBookSetService (5个) | grp.pt.service.bookset | grp.pt.service.facade |
| 49-50 | IAgencyManagerService, IAgencyManagerAffixService | grp.pt.service.agencyManager | grp.pt.service.facade |

**共计约 50 个接口文件**

#### Service 实现迁移（→ service/impl/）

| 序号 | 类名 | 原包名 | 新包名 |
|------|------|--------|--------|
| 1-11 | EleRelationServiceImpl 等 (11个) | grp.pt.service.impl | 保持不变 |
| 12-42 | AgencyServiceImpl ~ VirtualDataBaseServiceImpl (31个) | grp.pt.service.basedata.impl | grp.pt.service.impl |
| 43-49 | AccEleManagerServiceImpl ~ NoSessionBookSetServiceImpl (7个) | grp.pt.service.bookset.impl | grp.pt.service.impl |
| 50-52 | AgencyManagerServiceImpl 等 (3个) | grp.pt.service.agencyManager.impl | grp.pt.service.impl |

**共计约 52 个实现文件**

### 1.4 特殊处理事项

| 事项 | 说明 | 处理建议 |
|------|------|---------|
| `DBScripGenerateServiceImp` | 后缀 `Imp` 非标准 | 迁移时同步重命名为 `DBScripGenerateServiceImpl` |
| `FinanceStandardInitUtil` | 工具类混入 service/impl/ | 评估是否迁入 common/util/ |
| `service/agencyManager/constant/` | 常量类混入 service 包 | 评估是否迁入 common/constant/ 或 module/constant/ |
| `service/agencyManager/enums/` | 枚举混入 service 包 | 评估是否迁入 common/enums/ 或 module/enums/ |
| `service/agencyManager/model/` | Model 混入 service 包 | 评估是否迁入 model/dto/ |
| `service/agencyManager/util/` | 工具类混入 service 包 | 评估是否迁入 common/util/ |
| `service/agencyManager/feignclient/` | Feign 客户端混入 service 包 | 评估是否迁入 common/feign/client/ |
| `service/agencyManager/exception/` | 异常类混入 service 包 | 评估是否迁入 common/exception/ |
| `ExecutorMonitorService` | 在 service/executor/ 下 | 评估是否归入 facade/ 或保留 |
| `service/synchronization/` | 同步服务子包 | 评估是否归入 impl/ 或保留 |

### 1.5 影响评估

- **import 引用变更**：289 处引用，分布在 156 个文件中
- **跨模块影响**：element-server 和 element-server-com 均有大量引用
- **风险等级**：**中** — 仅改 package/import，但文件数量大，需逐一更新

---

## 二、DAO 层 mapper/entity 分离

### 2.1 当前结构问题

**DAO 层：**
```
element-server-com/src/main/java/grp/pt/dao/
├── EleRelationDAO.java                   ← 根目录 DAO 接口（5个）
├── ElementValueDAO.java
├── IOrgFeignDAO.java
├── IProcessElementDao.java
├── InformationCheckDao.java
├── basedata/                             ← 按业务分组（40+接口）
│   ├── IAgencyDao.java
│   ├── IAnnualContrastDao.java
│   ├── ... (共 30 个接口)
│   └── impl/                             ← 业务分组下的实现（31+个）
│       ├── AnnualContrastDaoImpl.java
│       ├── BaseDataCommonDao.java
│       └── ...
├── bookset/                              ← 账套分组（2接口 + 5实现）
│   ├── IAccEleManagerDao.java
│   ├── IAgencyEleManagerDao.java
│   └── impl/
├── agencyManager/                        ← 单位管理分组（2接口 + 3实现）
│   ├── IAgencyManagerAffixDao.java
│   ├── IAgencyManagerDao.java
│   └── impl/
└── impl/                                 ← 根级实现（13个）
    ├── JDBCEleRelationDaoImpl.java
    ├── JdbcElementValueDaoImpl.java
    ├── EleRelationMapper.java            ← Mapper 混入 dao/impl
    ├── EleRelationDetailMapper.java      ← Mapper 混入 dao/impl
    ├── ProcessElementDaoImpl.java
    ├── CommonBaseDataImportDao.java
    ├── ElementValueSetDAO.java
    ├── InformationCheckDaoImpl.java
    ├── OrgFeignDaoImpl.java
    └── ...
```

**Mapper 层（独立包）：**
```
element-server-com/src/main/java/grp/pt/mapper/      ← 独立顶层包，非 dao/mapper/
├── ElementMapper.java                                ← 根目录（2个）
├── EnumValueMapper.java
└── basedata/                                         ← 按业务分组（19个）
    ├── AccElementMapper.java
    ├── AgencyElementMapper.java
    ├── BasMofDivMapper.java
    ├── CatalogMapper.java
    ├── CateTypeMapper.java
    ├── DataElementMapper.java
    ├── ElePortalSystemMapper.java
    ├── ElementDTOMapper.java
    ├── EleUnionMapper.java
    ├── ElementMapper.java
    ├── ElementValueComplexMapper.java
    ├── ElementValueExtendMapper.java
    ├── ElementValueMapper.java
    ├── ElementValueSimplyMapper.java
    ├── FieldMapper.java
    ├── SubSystemMapper.java
    ├── TableCatalogMapper.java
    ├── TableMapper.java
    └── VirtualDataBaseMapper.java
```

### 2.2 目标结构

```
dao/
├── mapper/                     # 所有 MyBatis Mapper 接口
│   ├── ElementMapper.java      (原 grp.pt.mapper)
│   ├── EnumValueMapper.java
│   ├── EleRelationMapper.java  (原 grp.pt.dao.impl)
│   ├── EleRelationDetailMapper.java
│   └── basedata/
│       ├── AccElementMapper.java
│       └── ... (19个)
├── entity/                     # 持久化实体（待整理）
│   └── (从 model/ 中迁入 Entity 类)
├── basedata/                   # DAO 接口保持业务分组
│   ├── IAgencyDao.java
│   └── ...
├── bookset/
│   ├── IAccEleManagerDao.java
│   └── ...
├── agencyManager/
│   ├── IAgencyManagerDao.java
│   └── ...
└── impl/                       # DAO 实现统一归入
    ├── JDBCEleRelationDaoImpl.java
    ├── JdbcElementValueDaoImpl.java
    └── ...
```

### 2.3 迁移清单

#### Mapper 迁移（grp.pt.mapper → grp.pt.dao.mapper）

| 序号 | 类名 | 原包名 | 新包名 |
|------|------|--------|--------|
| 1 | ElementMapper | grp.pt.mapper | grp.pt.dao.mapper |
| 2 | EnumValueMapper | grp.pt.mapper | grp.pt.dao.mapper |
| 3-21 | AccElementMapper ~ VirtualDataBaseMapper (19个) | grp.pt.mapper.basedata | grp.pt.dao.mapper.basedata |

**共计 21 个 Mapper 文件**

#### 混入 dao/impl 的 Mapper 迁移

| 序号 | 类名 | 原包名 | 新包名 |
|------|------|--------|--------|
| 22 | EleRelationMapper | grp.pt.dao.impl | grp.pt.dao.mapper |
| 23 | EleRelationDetailMapper | grp.pt.dao.impl | grp.pt.dao.mapper |

#### Entity 目录创建

- 创建 `dao/entity/` 目录
- 从 `model/` 中迁入明确的 Entity 类：`MasterDataEntity.java`

### 2.4 特殊处理事项

| 事项 | 说明 | 处理建议 |
|------|------|---------|
| 无 @MapperScan 注解 | 项目无显式 @MapperScan 配置 | Mapper 接口可能通过 Spring Boot 自动扫描或其他方式注册，需确认 |
| 无 MyBatis XML | 未发现 Mapper XML 文件 | Mapper 使用注解方式，无需更新 XML namespace |
| dao/impl/ 下混入 Mapper | EleRelationMapper, EleRelationDetailMapper | 迁入 dao/mapper/ |
| DAO 根目录接口 | 5 个接口直接在 dao/ 下 | 建议保留或迁入 dao/basedata/ 等业务分组 |

### 2.5 影响评估

- **Mapper import 引用**：24 处引用，分布在 24 个文件中
- **风险等级**：**高** — 需确认 Mapper 扫描机制，包路径变更可能导致 Bean 注册失败
- **建议**：迁移后立即编译验证，确认 Mapper Bean 正常注册

---

## 三、Model 层 dto/vo/query 分类

### 3.1 当前结构问题

```
element-server-com/src/main/java/grp/pt/model/
├── ElementValue.java               ← 根目录未分类文件（8个）
├── ElementValueConfig.java
├── ElementValueConfig_new.java     ← _new 后缀，疑似临时文件
├── ElementValueDetall.java         ← 拼写错误 Detall（应为 Detail）
├── ElementValueSetter.java
├── ElementValue_new.java           ← _new 后缀，疑似临时文件
├── MasterDataEntity.java           ← Entity 类错放在 model/ 下
├── ValueSetElement.java
└── dto/                            ← 已有 dto/ 目录（2个文件）
    ├── ValueSetDTO.java
    └── DBExportToolDTO.java
```

**另外：grp-element-api 模块也有 model/**
```
grp-element-api/src/main/java/grp/pt/common/model/
├── ElementDTO.java                 ← 跨模块 DTO
├── AgencyElement.java              ← 跨模块 Model
└── AccElement.java                 ← 跨模块 Model
```

### 3.2 目标结构

```
model/
├── dto/                    # 数据传输对象
│   ├── ValueSetDTO.java    (已在位)
│   ├── DBExportToolDTO.java (已在位)
│   ├── ElementValue.java   (从根目录迁入)
│   ├── ElementValueConfig.java
│   ├── ElementValueSetter.java
│   └── ValueSetElement.java
├── vo/                     # 视图对象（新建目录）
│   └── (根据业务需要归入)
└── query/                  # 查询条件对象（新建目录）
    └── (根据业务需要归入)
```

### 3.3 迁移清单

| 序号 | 类名 | 原位置 | 建议位置 | 说明 |
|------|------|--------|---------|------|
| 1 | ElementValue | model/ | model/dto/ | 数据传输对象 |
| 2 | ElementValueConfig | model/ | model/dto/ | 配置数据 |
| 3 | ElementValueConfig_new | model/ | **建议删除或合并** | 临时文件，需确认 |
| 4 | ElementValueDetall | model/ | model/dto/ | 需确认拼写是否修正 |
| 5 | ElementValueSetter | model/ | model/dto/ | 值设置器 |
| 6 | ElementValue_new | model/ | **建议删除或合并** | 临时文件，需确认 |
| 7 | MasterDataEntity | model/ | dao/entity/ | Entity 类应归入 DAO 层 |
| 8 | ValueSetElement | model/ | model/dto/ | 值集元素 |

### 3.4 特殊处理事项

| 事项 | 说明 | 处理建议 |
|------|------|---------|
| `ElementValueConfig_new` | `_new` 后缀，疑似临时文件 | 需用户确认：是否废弃、合并到 ElementValueConfig |
| `ElementValue_new` | `_new` 后缀，疑似临时文件 | 需用户确认：是否废弃、合并到 ElementValue |
| `ElementValueDetall` | 拼写错误（Detall → Detail） | 需用户确认是否修正为 ElementValueDetail |
| `MasterDataEntity` | Entity 类放在 model/ | 应迁入 dao/entity/ |
| `grp-element-api` 的 model | 跨模块 ElementDTO 等 | 属于 API 模块，暂不迁移 |
| model/ 无 vo/ 目录 | 缺少视图对象分类 | 先创建空目录，后续按需归入 |
| model/ 无 query/ 目录 | 缺少查询条件分类 | 先创建空目录，后续按需归入 |

### 3.5 影响评估

- **Model import 引用**：379 处引用，分布在 253 个文件中
- **跨模块影响**：element-server、element-server-com、grp-element-api 均有引用
- **风险等级**：**高** — 影响面最广，需极其谨慎
- **建议**：分批执行，先处理明确的分类（MasterDataEntity → dao/entity/），再处理根目录文件

---

## 四、执行优先级建议

| 优先级 | 层级 | 理由 |
|--------|------|------|
| 1 | **Model 层** — 创建 vo/、query/ 空目录 + MasterDataEntity 迁入 dao/entity/ | 影响最小，仅 1 个文件迁移 |
| 2 | **DAO 层** — Mapper 迁入 dao/mapper/ | 影响面可控（24 个引用），但需验证 Bean 注册 |
| 3 | **Model 层** — 根目录文件归类到 dto/ | 影响面大（253 个文件），需确认 _new 文件处置 |
| 4 | **Service 层** — facade/impl 分离 | 影响面大（156 个文件），agencyManager 子包结构需整理 |

---

## 五、约束限制项

以下问题**不在本次 P2 修复范围**：

| 问题 | 约束原因 |
|------|---------|
| `grp-element-api` 模块的 model 结构 | 跨模块公共 API，影响外部调用方 |
| DTO 属性下划线命名 | 序列化兼容性 |
| Service 接口命名前缀不统一（I 前缀 vs 无前缀） | 历史约定，影响范围过大 |
| DAO 接口命名不统一（I 前缀 vs 无前缀，Dao vs DAO） | 历史约定 |

---

## 六、执行前检查清单

- [ ] 确认项目可编译通过（当前基线）
- [ ] 确认 Mapper Bean 注册机制（@MapperScan 路径或自动扫描范围）
- [ ] 确认 `_new` 后缀文件是否可删除/合并
- [ ] 确认 `ElementValueDetall` 拼写是否需修正
- [ ] 确认 `service/agencyManager/` 下非 Service 子包（constant/enums/model/util/exception/feignclient）的目标位置
- [ ] 建议在独立分支上执行，便于回退
